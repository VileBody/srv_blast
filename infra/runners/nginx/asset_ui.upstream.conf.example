# Upstream for asset-ui (FastAPI + Vite UI).
# Container listens on 127.0.0.1:18173 (see docker-compose.yml -> asset-ui.ports).
upstream blast_asset_ui {
    server 127.0.0.1:18173;
    keepalive 16;
}
