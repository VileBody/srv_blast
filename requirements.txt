# requirements.txt
# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Queue / background jobs
celery>=5.4.0
redis>=5.0.0

# Config / templates
python-dotenv>=1.0.1
jinja2>=3.1.4

# Models / validation
pydantic>=2.7.0
# CPU-only subject detection for one-time PHOTO framing (no Torch/Ultralytics).
opencv-python-headless==4.10.0.84

# Gemini client (google.genai)
google-genai>=0.6.0

# F5 Cognition («Мысль») hook — decode/measure/encode the TTS wav. Runs in the
# orchestrator build subprocess (runtime image), needs the ffmpeg system binary
# (installed in the base Dockerfile). NOT in requirements.infra.txt — the slim
# bots don't run F5.
pydub>=0.25.1

# Retries
tenacity>=8.2.3

# Telegram Bot API service
aiogram>=3.15.0
telethon>=1.36.0
aiohttp-socks>=0.10.1
httpx[socks]>=0.27.0  # [socks] pulls in socksio — needed for SOCKS-proxy file downloads
boto3>=1.35.0
librosa>=0.10.2
asyncpg>=0.29.0
python-multipart>=0.0.9
prometheus-client>=0.20.0
docker>=7.1.0

# F6 «Прогрев видео», ветка ссылки на YouTube (эндпоинт /media/fetch_external).
# yt-dlp живёт только в runtime-образе: слим-образы ботов его не тянут, они
# ходят за вырезкой в оркестратор. Версию НЕ пинуем: экстракторы протухают за
# 1–2 недели, пин здесь означал бы плановую поломку фичи.
yt-dlp>=2026.1.1
# Плагин, который забирает PO-токены у сайдкара bgutil (сервис ytdlp-pot).
# Без токена YouTube отдаёт 403 / пустой список форматов.
bgutil-ytdlp-pot-provider>=1.1.0
