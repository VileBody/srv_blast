{# templates/text_template.j2 #}
logLine("INFO", "TEXT_TEMPLATE: layers=" + (text_layers ? text_layers.length : 0));

for (var i = text_layers.length - 1; i >= 0; i--) {
  createLayerFromBlueprint(text_layers[i]);
}
